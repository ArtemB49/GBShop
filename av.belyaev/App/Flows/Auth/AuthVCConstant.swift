/**
 * Константы AuthViewController
 */

extension AuthViewController {
    
    enum AuthConstants: String {
        case unwindToUserData
        case alertTitle = "Авторизация"
        case alertMessage = "Неверный логин или пароль"
        case defaultPasswordAndUser = "test"
    }
}
